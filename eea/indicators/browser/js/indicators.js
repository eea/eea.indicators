$(document).ready(function () {
		// $(".kupu-fulleditor").();
		$(".kupu-table").remove();
		$('.kupu-image').remove();
		// $(".kupu-tb-styles option[value='h2|']").remove();
		// $(".kupu-tb-styles option[value='h3|']").remove();

		// kupu.tools.ui.paraoptions = [["Mememe", 'h5|']]
});

